# CT-3
# Задача 1.
Реализовать функцию swal для обмена значений между аргументами x и y, не
используя при этом третьей переменной.
# Задача 2.
Реализовать обобщенную функцию печати значения функции от аргумента
void print(float x, float (*y) (float))
# Задача 3.
Реализуйте перегрузку функций max и min для массива из k вещественных
чисел. С помощью программы найдите произведение минимумов двух
массивов различной длины и среднее геометрическое их максимумов.
# Задание 4*.
Составить алгоритм и программу, реализующую разные механизмы передачи
параметров, на примере нахождения значений функций:<br>
1) функция принимает параметры по значению и возвращает значение;<br>
2) функция в качестве параметров принимает указатели, в том числе и
параметр адреса результата, а сама функция типа Void.<br>
3) функция принимает параметры по ссылке и возвращает ссылку на
результат.<br>
Функция вычисляет сумму членов ряда. Вычисление суммы необходимо
выполнять до тех пор, пока очередное слагаемое (член ряда) по модулю не
станет меньше заданной точности e = 10-4. x=0.20, e=0.5 * pow(10, -4)
# Задача 1.
Вычислить значение 𝑥 = √𝑎, используя формулу
𝑥𝑛 = 1
2 (𝑥𝑛−1 + 𝑎
𝑥𝑛−1
)
В качестве начального приближения использовать значение 𝑥0 = 1+𝑎
2 .
Код программы представлен ниже. Вставьте пропуски, объясните работу
программы и назначение параметров.
#include <iostream>
using namespace std;
double sqrt_rec(double a, int tochnost) {
if (tochnost == 0)
return ????;
else
{
????
return ???? ;
}
}
void main() {
double x = 2;
cout << sqrt(x) << endl;
int t = 6;
cout << sqrt_rec(x, t) << endl;
}
# Задача 2
Вычислить произведение n ≥ 2 (n четное) сомножителей
𝑦 = 2
/1 ∙ 2
/3 ∙ 4
/3 ∙ 4
/5 ∙ 6
/5 ∙ 6
/7 …
# Задача 3.
Вывести массив в обратном порядке.
# Задание 4.
Дано натуральное число N. Выведите слово YES, если число N является
точной степенью двойки, или слово NO в противном случае. Операцией
возведения в степень пользоваться нельзя!
# Задача 5.
В теории вычислимости важную роль играет функция Аккермана A(m,n),
определенная следующим образом:
Даны два целых неотрицательных числа m и n, каждое в отдельной строке.
Выведите A(m,n).
Некоторые значения приведены в таблице. Функция растет очень быстро.
# Задание 6.
Вычислить 𝑦 = 𝑥𝑎 по следующему алгоритму:
𝑦 = (𝑥𝑎 2⁄ )2
, если a – четное
𝑦 = 𝑥 ∙ 𝑥𝑎−1, если a – нечетное
# Задание 7.
Дано натуральное число N. Вычислите сумму его цифр. При решении этой
задачи нельзя использовать строки, списки (вектора), массивы, циклы.
# Задание 8*.
Дана последовательность натуральных чисел (одно число в строке),
завершающаяся числом 0. Определите, какое количество элементов этой
последовательности, равны ее наибольшему элементу
